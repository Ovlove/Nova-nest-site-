---
// src/components/SubscribeBox.astro
---

<section class="bg-gray-100 rounded-2xl p-6 md:p-8 text-center space-y-4">
  <h2 class="text-2xl font-semibold text-gray-800">Stay in the Loop</h2>
  <p class="text-gray-600 max-w-xl mx-auto">
    Subscribe to get the latest posts, tools, and insights straight to your inbox.
  </p>
  <form
    class="flex flex-col sm:flex-row justify-center items-center gap-3 max-w-md mx-auto"
    onsubmit="event.preventDefault(); alert('Subscription feature coming soon!')"
  >
    <input
      type="email"
      required
      placeholder="Enter your email"
      class="w-full px-4 py-2 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-primary"
    />
    <button
      type="submit"
      class="bg-primary text-white px-5 py-2 rounded-md hover:bg-blue-700 transition"
    >
      Subscribe
    </button>
  </form>
</section>
