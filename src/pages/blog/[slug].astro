---
import Layout from '../../layouts/BaseLayout.astro';
import { getEntryBySlug } from 'astro:content';
import { AstroError } from 'astro/errors';

const { slug } = Astro.params;
const post = await getEntryBySlug('blog', slug);

if (!post) {
  throw new AstroError(`Post not found for slug: ${slug}`);
}

const { Content, data, body } = post;

// Estimate reading time
const wordCount = body ? body.split(/\s+/).length : 0;
const readingTime = Math.ceil(wordCount / 200);
---

<Layout title={data.title} description={data.description}>
  <main class="px-4 md:px-8 lg:px-16 py-12 max-w-4xl mx-auto">
    <article class="space-y-6">
      <header class="space-y-2">
        <h1 class="text-4xl font-bold text-gray-800">{data.title}</h1>
        <div class="text-sm text-gray-500 space-x-3">
          <span>{new Date(data.pubDate).toDateString()}</span>
          <span>• {readingTime} min read</span>
          {Array.isArray(data.tags) && data.tags.length > 0 && (
            <span>
              • {data.tags.map(tag => (
                <span class="inline-block bg-blue-100 text-blue-700 px-2 py-0.5 rounded text-xs mr-1">{tag}</span>
              ))}
            </span>
          )}
        </div>
      </header>

      <div class="prose prose-lg max-w-none prose-headings:text-gray-800 prose-p:text-gray-700 prose-a:text-blue-600 hover:prose-a:text-blue-800">
        <Content />
      </div>

      <footer class="mt-12 border-t pt-6">
        <div class="flex items-center space-x-4">
          <img src="/author.png" alt="Author" class="w-12 h-12 rounded-full object-cover" />
          <div>
            <p class="text-sm font-semibold text-gray-800">Written by NovaNest Team</p>
            <p class="text-sm text-gray-500">Sharing insights on tech, tools, and income strategies.</p>
          </div>
        </div>
      </footer>
    </article>
  </main>
</Layout>
