---
import Layout from '../../layouts/BaseLayout.astro';
import { getEntryBySlug } from 'astro:content';
import { AstroError } from 'astro/errors';

const { slug } = Astro.params;
const post = await getEntryBySlug('blog', slug);

if (!post) {
  throw new AstroError(`Post not found for slug: ${slug}`);
}

const { Content, data } = post;
---

<Layout title={data.title} description={data.description}>
  <main class="px-4 md:px-8 lg:px-16 py-12 max-w-4xl mx-auto">
    <article class="space-y-6">
      <header class="space-y-2">
        <h1 class="text-4xl font-bold text-gray-800">{data.title}</h1>
        <p class="text-sm text-gray-500">{new Date(data.pubDate).toDateString()}</p>
      </header>

      <div class="prose prose-lg max-w-none prose-headings:text-gray-800 prose-p:text-gray-700 prose-a:text-blue-600 prose-a:underline hover:prose-a:text-blue-800">
        <Content />
      </div>
    </article>
  </main>
</Layout>
